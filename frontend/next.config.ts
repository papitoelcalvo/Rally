/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  typescript: {
    ignoreBuildErrors: false, // Asegúrate de que los errores de TypeScript no se ignoren
  },
};

module.exports = nextConfig;
